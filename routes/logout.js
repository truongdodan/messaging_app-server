const express = require("express");
const router = express.Router();
const logoutController = require("../controllers/auth/logoutControllert");

// router.route("/").post(logoutController.);

module.exports = router;
